#include <LiquidCrystal.h>

LiquidCrystal lcd(7, 8, 9, 10, 11, 12); // ajuste conforme seus pinos

void setup() {
  lcd.begin(16, 2);
  lcd.print("GUI GAY");
}

void loop() {
  lcd.setCursor(0, 1);
  lcd.print("AURA GAY: ");
  lcd.print(millis() / 1000); // mostra segundos ligados
  delay(1000);
}
